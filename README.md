# yinggarykairui.github.io

Portfolio site for Kairui (Gary) Ying — a single static `index.html`, no dependencies, no build step.
It self-updates on every load from the build factory's dashboard (featured builds, stats, full build history).
Maintained by the build factory — [factory-hub](https://github.com/yinggarykairui/factory-hub).
